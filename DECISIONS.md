# DECISIONS

2026-06-17 - Project form: create a standalone paper under `papers/Difference-in-Differences_for_Corpus_Linguistics/`. Reason: the project is a distinct corpus-methods paper rather than a subproject of an existing corpus or causal-inference paper.

2026-06-17 - Working title: *Difference-in-differences for corpus linguistics: Causal inference, corpus composition, and linguistic change after shocks*. Reason: the title makes the paper a methods bridge rather than a generic introduction to DiD.

2026-06-17 - Infrastructure: use central `references.bib` and central `.house-style/` files by symlink. Reason: Brett requested no local house-style copy, and project-specific sources should stay in `references-local.bib` only after verification.

2026-06-17 - Citation discipline: do not add DiD or language-application citations from the seed note until they have been verified against authoritative sources. Reason: the paper depends on precise method claims and current DiD literature.

2026-06-17 - Drafting state: create section scaffolding and verification notes only; do not turn the seed note into a prose summary at setup time. Reason: Brett requested infrastructure, not a summary.

2026-06-17 - Advisory board (real experts to pressure-test the idea, not simulated journal referees): Katherine Keith (text-as-confounder/measurement), Jonathan Roth (parallel trends), Scott Cunningham (DiD exposition/identification), Andrew Gelman (adversary, DiD-vs-modeling), Jacob Eisenstein (linguistics bridge, diffusion). Saved in `advisory-board.md`. Reason: the goal is getting the design right before drafting; these five cover measurement, identification, modern-DiD practice, adversarial stress, and how change actually diffuses.

2026-06-17 - Ran the board over the plan via two redundant LLM panels (Opus subagents + Codex CLI, outputs in `reviews/review-board-20260617-091034/`). Verdict 10/10 "promising but needs reframing." Cross-model steers, in priority order: (1) commit to ONE estimand, naming which of three targets it is (population language use / corpus-contributor use / corpus textual composition); (2) choose the worked example first and prove it is identified (named never-treated comparison with a defensible counterfactual), else it is interrupted time series, not DiD; (3) frame DiD as the identification design and a hierarchical count model as the estimator within it, not as rivals; (4) give parallel trends its own section and make "composition drift contaminates the very pre-trend test meant to detect it" the signature contribution; (5) demote modern-DiD/staggered material (07) unless the example has staggered timing, and add an honest uncertainty story for the N-of-2, short-pre-period reality. Reason: both panels independently converged on these; logging before any reframing of the outline.

2026-06-17 - Estimand: target (a), a change in language use in a population (not corpus-contributor use, not corpus composition). Reason: it is the quantity linguists care about. Cost accepted: corpus frequency = population rate + composition bias, so the identifying assumption becomes parallel trends in the composition-induced measurement bias, not just in the underlying rate; the constructive response is reweighting/post-stratification to stable composition marginals before differencing.

2026-06-17 - Fork resolved by the estimand: DiD is the identification spine (keeps the causal claim auditable for a linguistics audience), and a hierarchical count / reweighting model is the estimator within it. Not rivals. Reason: a population estimand requires both a measurement layer and an identification design.

2026-06-17 - Worked example: simulation is the methodological spine (only place a population estimand can be validated, since population truth is never observed in real data); prescriptivism is the primary real running case; Americanization of British English is the second, harder case (diffuse pressure, no single treatment date, stresses identification and the staggered/event-study machinery). Orthographic-reform set-piece dropped as dull. Reason: prescriptivism gives a within-language control, naturally staggered treatment across guides/features, and a vivid composition threat, and sits in Brett's wheelhouse; Americanization carries the live media-and-change and Americanization debates with the Brown-family corpora.

2026-06-17 - that/which round (now superseded). After a verification pass, briefly adopted the restrictive that/which alternation, US (treated) vs UK (control), as the centerpiece, building on Hinrichs, Bohmann & Szmrecsanyi (2015, Language). A second advisory-board round (Opus, 5 reviewers) rejected it 5/5: the treatment (copy-editing enforcement) acts on the same editing layer as the measurement (edited published prose), so it identifies the editorial regime / bias term, not population use; the UK is the rule's country of origin (Fowler), so it is lighter/earlier-treated, not a clean control; White 1959 is a focal fiction (anticipation; the rule predates it); and 2 varieties x ~4 Brown points give no power to test pre-trends. Architecture (estimand commitment, rate+bias decomposition, pre-trend-contamination signature point, simulation spine) survived intact; only the empirical centerpiece failed. Reason: the failure localized to the example, validating the round-1 architecture choices.

2026-06-17 - Worked-example pivot to "cross-variety language policy as the natural-experiment template" (the sibling variety supplies the control that English-internal designs lack). Three-tier structure: simulation (works in principle) + feminization of profession nouns, Quebec/France (works in practice, CENTERPIECE) + Croatian/Serbian divergence after 1991 (breaks in practice, CAUTIONARY case where the political rupture confounds the corpus with the treatment). German 1996 spelling reform dropped (dull); that/which, English prescriptivism (less/fewer, double negation), and Americanization dropped as centerpiece. Reason: feminization reaches production (not the editing layer), has accessible non-genre-matched corpora and a real prior-art literature, and a sharp cross-variety policy gradient; Croatian/Serbian dramatizes the signature threat.

2026-06-17 - Verification pass on the new worked examples. FEMINIZATION (centerpiece) confirmed feasible: timeline verified (Quebec 1979; France Fabius circular 1986, Jospin circular 1998; Academie francaise accepted feminized job titles Feb 2019; Belgium 1993). Prior art to build on: Burnett & Bonami (2019, "Gender in French: a diachronic perspective"); Elmiger (2021); a Canada-vs-Europe "implantation of feminine forms" corpus comparison (Langage et societe, 2014) that is essentially our cross-variety design; the langsci-press book on Dutch/German feminising morphology shows the template travels. Accessible corpora: Europresse (Le Monde from 1944; newspapers 1990s+), frTenTen, Frantext, plus diachronic SPOKEN corpora (spINAch 1955-2015; Montreal/Paris/Marseille sociolinguistic interviews). Refinements: (a) feminization is naturally STAGGERED across varieties (Quebec 1979, Belgium 1993, Academie 2019), giving Section 07's heterogeneity-robust estimators real work; (b) triangulating edited written vs unedited spoken corpora directly addresses the editing-layer worry. Caveat: France is also later/weaker-treated, not never-treated, so use the multi-variety gradient / staggered framing, not a naive Quebec-vs-France 2x2. CROATIAN/SERBIAN (cautionary) confirmed substantively (Croatian purism strong in the 1990s, weaker after 2000; Langston & Peti-Stantic 2014) but data is deliberately hard: hrWaC/srWaC are web crawls from 2011/2014 with no pre-1991 coverage and no off-the-shelf diachronic BCS newspaper corpus, so a clean 1991 before/after needs building from archives. That difficulty confirms the cautionary role and is itself an instance of the thesis: the rupture that drives the change also disrupts the documentary record. Reason: centerpiece is feasible with prior art and accessible data; cautionary case is appropriately hard. Citations above need final verification before drafting.

2026-06-17 - Staggered timeline VERIFIED; design locked. Official feminization dates: Quebec 1979 (OQLF first avis); Suisse romande 1991 (Therese Moreau, Dictionnaire feminin-masculin, Geneva); Belgium 21 June 1993 (Communaute francaise decret; "Mettre au feminin" guide 1994); France 1986 (Fabius circular), 1998 (Jospin circular), 2019 (Academie). Italian parallel: Alma Sabatini, "Il sessismo nella lingua italiana" / "Raccomandazioni" (1987, commissioned by the Presidenza del Consiglio); later Crusca/Robustelli. DESIGN: adopt a staggered-adoption DiD (Callaway-Sant'Anna) across varieties by official date, using not-yet-treated varieties as controls, so no never-treated unit is needed (this dissolves the "France is not a clean control" problem). PROMOTE Italian to the cleanest single-shock co-case: Sabatini 1987 is a sharp government shock and the la Repubblica corpus (1985-2000, ~325M words) brackets it with a real pre-period and article-level resolution. Switzerland is the generalization engine: Swiss-AL covers French/German/Italian/Romansh Swiss media under one federal policy (depth for 1991 via Swissdox@LiRi still to confirm); each Swiss region pairs with its metropole (Romandie/France, Ticino/Italy, Swiss German/Germany). KEY POINT: this structure repairs the round-2 board's three killers of the that/which centerpiece -- staggered cross-variety controls (no contaminated never-treated unit), real time resolution (la Repubblica/Europresse fix the 4-point no-power problem), and a production-level lexical choice triangulatable with spoken corpora (not a pure editing reflex). Reason: verification confirms clean dates and bracketing corpora, and the design fixes what killed the prior centerpiece.

2026-06-17 - Key precedent found (literature intake): Burnett & Bonami (2019, Language in Society 48), "Linguistic prescription, ideological structure, and the actuation of linguistic changes: grammatical gender in French parliamentary debates." They already study the 1986 (Fabius) and 1998 (Jospin) feminization policies as treatments on feminine grammatical gender in French parliamentary (spoken) debate, finding NO effect in 1986 and a dramatic effect in 1998, attributed to a 1990s shift in gender ideology rather than the policy itself. POSITIONING CONSEQUENCE: the France 1986/1998 before/after is taken; our novelty must be the causal-design upgrade -- cross-variety staggered DiD (Quebec 1979 / Suisse romande 1991 / Belgium 1993 as not-yet-treated controls) plus the composition/measurement layer -- converting their within-France, ideology-explained account into an identified estimate with a control series they lacked. Must engage them directly (Rapoport). THREAT they hand us: the "treatment" (a feminization circular) is not a clean exogenous shock; its effect is ideology-moderated (identical policy, opposite outcomes in 1986 vs 1998), i.e., treatment-effect heterogeneity plus a time-varying confounder. This is the parallel-trends problem in its sharpest form and is now the paper's central identification question; it justifies the heterogeneity-robust estimators in Section 07. Corroboration: Fujimura (2005, full text) shows a sharp post-1998 jump for ministre/depute(e) and prestige/domain resistance (professeur, auteur, medecin) = composition conditioning; Flesch, Abbou & Burnett (2026, Linguistics) show ~5% generic masculine among activist speakers = sample-dependence sharpening the population-estimand caveat. Reason: intake located the closest prior art; it reshapes positioning and surfaces the central identification threat.

2026-06-17 - Correction + full read of the key precedent. (a) Citation fix: "Gender in French: a diachronic perspective" is Juhani Harma (a descriptive historical overview of the French gender system, Latin to Modern, neutralization; background only, NOT a quantitative feminization study) -- an earlier log line mis-attributed this title to Burnett & Bonami. (b) Burnett & Bonami (2019, Language in Society) now read in full: corpus = Assemblee nationale transcripts 1982-2017, 99,480 female terms of address (Madame le/la N); outcome = proportion feminine; method = GLMM (lme4) with speaker random effect and fixed effects date (before/after 6 March 1998), party, age, social gender, function noun. Findings: Fabius 1986 ~no effect; Jospin 1998 dramatic rise (43% feminine in the Sept 1997-July 1998 window); function-noun prestige hierarchy (depute(e) .74 > president(e) .70 > secretaire d'Etat .52 > ministre .44 > garde des sceaux .20); strong party effect (left ~64% vs right ~28%); explanation = emergence of a "differentialist feminine persona" via the parite movement, so the policy worked only because ideology had already shifted. THREE design takeaways: (1) reusable measurement trick -- terms of address Madame le/la N auto-identify female referents, no hand-coding; (2) B&B already use the GLMM estimator our design endorses, but WITHOUT a cross-variety control or DiD/parallel-trends framing -- that gap is our contribution; (3) their transcription-reliability check (85% vs FR3 video, errors clustered at the change's start/end) shows the edited compte rendu may exaggerate abruptness -- a measurement/editing-bias instance for our composition thesis. Central threat reconfirmed: the policy is endogenous to ideology, so cross-variety DiD must defend parallel ideology trends or model ideology directly. Reason: precedent read in full; sharpens both contribution and threat.

2026-06-17 - Feminization intake complete (to open-web limit). New: Elizabeth Dawes (2003, Ethnologies), "La feminisation des titres et fonctions dans la Francophonie: de la morphologie a l'ideologie" -- a cross-variety comparison (France / Belgium / Switzerland / Canada) showing the varieties realize feminization via DIFFERENT morphological strategies (France resists; Belgium epicene un/une professeur; Switzerland -esse; Canada graphic -eure: docteure/chercheure), and that prestige/ideology, not morphology, drives resistance ("une secretaire" but not "une secretaire d'Etat"). NEW MEASUREMENT THREAT for the design: a cross-variety DiD cannot compare raw "feminine-form" rates directly, because the same feminization surfaces as different forms across varieties; define the outcome strategy-agnostically (any feminine marking on the title) or model each strategy separately, else the comparison conflates morphological strategy with feminization rate. Funtek (2014, Univ. of Oslo MA thesis, France-vs-Quebec press) obtained at abstract level only (full OA PDF at DUO handle 10852/39915, "MA_FeminisationNomsDeMetiers.pdf", convertible if detailed rates are needed); note it dates Quebec official feminization to 1976 vs the 1979 OQLF avis logged earlier -- reconcile before citing. Flesch, Abbou & Burnett (2026) remains headline-only (~5% generic masculine among activist speakers; full PDF bot-blocked). Reason: intake complete; Dawes adds a cross-variety comparability threat to the outcome definition.

2026-06-17 - Methods literature intake (abstract-level). DiD econometrics: Goodman-Bacon (2021, decomposition: TWFE = weighted avg of 2x2 DiDs), de Chaisemartin & D'Haultfoeuille (2020, AER: TWFE = weighted sum of ATTs with possibly negative weights), Sun & Abraham (2021, event-study contamination + flat pre-trends as heterogeneity artifacts), Callaway & Sant'Anna (2021, group-time ATTs under conditional parallel trends, doubly-robust), Borusyak/Jaravel/Spiess (2024, imputation estimator), Roth/Sant'Anna/Bilinski/Poe (2023, modern-DiD synthesis), Rambachan & Roth (2023, sensitivity analysis under bounded PT violations, HonestDiD), Bertrand/Duflo/Mullainathan (2004, serial correlation biases DiD SEs). Text-as-data causal: Keith/Jensen/O'Connor (2020, text-as-confounder review), Egami/Fong/Grimmer/Roberts/Stewart (2022, latent-representation risk + split-sample workflow), Feder et al. (2022 TACL, the causal-NLP survey, co-authored by board members Keith and Eisenstein). DESIGN CHOICES: (1) Section 07 workhorse = Callaway & Sant'Anna (staggered + parallel-trends-after-reweighting matches our design exactly), with Goodman-Bacon + de Chaisemartin-D'Haultfoeuille as the TWFE-fails diagnosis and Sun-Abraham/Borusyak as robustness; (2) parallel-trends section = Rambachan-Roth sensitivity bounds, the honest response to the B&B ideology confounder (don't assert PT, bound under violation); (3) inference = Bertrand-Duflo-Mullainathan warning (few varieties/clusters + autocorrelated frequency series = unreliable naive SEs; use cluster-robust or randomization inference; Gelman's N-of-2); (4) measurement layer (Section 05) = Egami split-sample workflow + Keith/Feder, the formal version of the composition/measurement section. NICHE restated from the methods side: the DiD literature assumes a clean outcome; the text-causal literature assumes a noisy latent outcome; the corpus case needs both and neither alone covers it. Full reads warranted before implementing Callaway-Sant'Anna, Rambachan-Roth, and Egami et al. Reason: both literature halves now mapped; estimator, sensitivity, inference, and measurement tools chosen.

2026-06-17 - Round-3 advisory board (Opus, 5 reviewers) on DESIGN-SPEC.md v1. Verdict 3 "still needs design work" (Roth, Gelman, Eisenstein) + 2 "draft once central fix made" (Keith, Cunningham); architecture endorsed. DEEPER FINDING than round 2 (5/5): the cross-variety pivot fixed the round-2 editing-layer mismatch but exposed selection-into-treatment-timing -- the continental gender-ideology wave that constitutes the effect (B&B) does not respect borders or policy dates, and treatment timing is selected on it, so not-yet-treated controls ride the same wave; Rambachan-Roth bounds a PT-violation magnitude but cannot fix selection into timing. Eisenstein added the formal underpinning: SUTVA / no-interference is violated via shared francophone media, the AFP wire, and Paris norm-deference. RESOLUTION (Roth + Cunningham, independently): reframe around the POLICY-TIME vs CALENDAR-TIME test; make the parite ideology wave the parallel trend differenced out, not the violation; elevate France 1986-null to the PRIMARY internal-validity placebo. Other consensus fixes folded into v2: commit treatment-date theory = policy date; state+stress-test SUTVA (AFP/shared-wire diagnostic + simulation interference arm); stop claiming powered inference (N~4-5 clusters; attach SE to variety, structured comparison with bounds); split two estimands (edited-standard via press vs population via spoken only, spoken triangulation load-bearing with a decision rule); test acceleration not a level jump (fix the simulation's logistic-pi-but-level-ATT inconsistency); resolve outlet-vs-variety with a two-outlet diagnostic; operationalize PT-in-b via composition-margin placebos; one sensitivity envelope; drop/restate Egami split-sample (no learned representation in a hand-detectable feature). Reason: round-3 endorsed the architecture but required these revisions.

2026-06-17 - Orienting principle (Brett): the paper improves METHODS; it is not trying to prove a linguistic or political finding about feminization. The worked examples are vehicles. Consequence: the round-3 threats are the CURRICULUM (each threat = a lesson + a diagnostic), not obstacles; the example need not yield a publishable causal estimate, only demonstrate the diagnostics honestly and model the downgrading of claims when identification fails. This stabilizes feminization as the example (rich, fully diagnosable threat set) and recasts the round-3 "still needs design work" verdicts, which graded it as an empirical claim -- the wrong rubric.

2026-06-17 - DESIGN-SPEC v2: re-centered the worked-example spine from "four-way staggered" to a PROGRESSIVE BUILD of designs on the feminization phenomenon, after Brett flagged over-commitment to a single complex design. Rungs: (1) Swiss French vs France -- the clean 2x2 core: same language (minimal Dawes morphological-comparability problem), large same-language control, shared continental wave differenced out, the policy-vs-calendar-time test sharp (does Suisse romande diverge from France after ~1991 or track Paris?), signature threat = SUTVA via shared francophone media/AFP, few-clusters honesty introduced cheaply; (2) scale to staggered multi-variety (add Quebec 1979, Belgium 1993) = Callaway-Sant'Anna, teaching what scaling COSTS (cross-country confounders, Dawes strategy divergence, few clusters, selection-into-timing), with France 1986-null as the placebo; (3) trilingual within-Switzerland = the design-choice contrast that holds country confounders constant by varying language (compact illustration only; trilingual data/competence out of scope, cross-language non-comparability, linguistic-finding temptation). Plus the simulation spine (now with interference and strategy-mix-drift arms, acceleration not level), Italian Sabatini 1987 as the sharper-event positive contrast, and Croatian/Serbian as the breakdown case. DEEPEST CONTRIBUTION = design choice (same phenomenon, increasingly demanding designs, honest about what each identifies). Reason: the over-commitment was to starting at complexity, not to feminization or French; building the design up teaches design choice better than perfecting one design, and the Swiss-French-vs-France core is cleaner and lower-cost (French only) than a four-way staggered start.

2026-06-17 - Section-11 items cleared + rung 1 drafted. CLEARED: rung-1 Swiss date = 1991 (Federal Chancellery report on non-sexist administrative language + Moreau's professions dictionary); Swissdox depth confirmed (holdings to 1911; Journal de Geneve / Gazette de Lausanne via the Le Temps archive cover pre-1991 Suisse romande); Quebec = 1979 (OQLF avis, Gazette officielle 28 July 1979; 1976 informal); France coding = policy date. Rambachan-Roth used in BREAKDOWN mode (sweep the violation size, calibrate from B&B), because the discrete ideology shock leaves no pre-period footprint to anchor relative-magnitude/smoothness restrictions -- itself a teaching point. RANDOMIZATION NULL TESTING (Brett) added as a first-class diagnostic and as the few-clusters inference: permute treatment / scramble labels; an honest procedure recovers no effect. PARTIAL POOLING (Brett) added as the estimator for unequal/small corpora: shrink the nuisance dimensions (outlet, profession, period within a variety) so small corpora (Suisse romande, Ticino) borrow strength, but keep FIXED effects at the variety/treatment level (selection-into-timing = RE-vs-FE / Hausman); scope = cures small-corpus noise, not the few-clusters identification problem. Rung 1 (Suisse romande treated 1991 vs France not-yet-treated until 1998, clean 2x2 baseline) drafted into sections/08-worked-example.tex; bib entries Burnett&Bonami 2019, Dawes 2003, Bertrand-Duflo-Mullainathan 2004, Moreau 1991 added to references-local.bib. Reason: clearing open items, incorporating Brett's null-testing and partial-pooling points, and beginning prose.

2026-06-17 - Codex board (redundant, cross-model) on DESIGN-SPEC v2 + rung-1 draft. Verdict UNANIMOUS 5/5 "draft once the central fix is made" -- more positive than the Opus round-3 board (3 "still needs work"), confirming v2 moved the needle. Cross-model validation (both model families): the progressive build, the policy-time-vs-calendar-time reframe, and the partial-pooling scope (shrink nuisance / fix treatment, no claim it rescues identification) are sound. CENTRAL FIX (5/5): operationalize the named diagnostics into a DIAGNOSTIC-TO-CLAIM DECISION RULE (a ladder mapping each diagnostic outcome to: point estimate / bounded-or-descriptive / not identified), so breakdown sensitivity, randomization nulls, and few-cluster caveats become enforceable constraints rather than well-written disclaimers -- itself the methods-teaching payoff, and the template every later rung inherits. Three rung-1 prose fixes: (1) the randomization null is OVERCLAIMED at N=2 -- it is a falsification/sanity check, not "the right inference" (assistant's drafting error; 4/5 flagged sections/08 line 19); (2) France is not simply "untreated" -- it had the 1986 Fabius circular, so frame 1986 as a null/placebo policy, define the clean 1991-1997 identifying window, and treat the 1998 Jospin shift as a follow-on second event; (3) 1991 may be a waypoint not a step -- add a pre-1991 slope/lead check and a waypoint-vs-intervention rule. Smaller: define the interference estimand (direct Swiss effect vs total effect including diffusion; treat AFP share / French-media dependence / outlet norm-deference as EXPOSURE variables, not just exclusions); require form-specific secondary estimates (measurement invariance); within-variety outlet replication before a variety-level claim; keep partial pooling from smoothing over the editing artifact. Reason: v2 architecture validated cross-model; remaining work is to make the honesty enforceable (decision rules) and tighten three rung-1 overstatements before drafting the rest.

---

2026-06-17 - PROVENANCE OF IDEAS (recorded at Brett's request, for an honest division-of-labour history; individual entries above already tag some contributions, this consolidates them and should feed the eventual AI acknowledgement and the "I am responsible for all claims" statement). Models in play: Claude Opus 4.8 (Anthropic; the main working model, "the assistant" below); the OpenAI Codex CLI (redundant review boards); a GPT "pro" model (OpenAI; the origin query, exact version for Brett to confirm); check-style audits ran on Claude Haiku.

ORIGIN: the seed question -- has difference-in-differences been used in corpus linguistics? -- was Brett's, posed to a GPT pro model this morning; Brett does not recall the prompting context. That empirical question is still not cleanly answered (adjacent work exists, e.g. Burnett & Bonami come close without calling it DiD, but no settled literature search has been run).

DIRECTION-SETTING (all Brett): requesting a real expert board (naming Grieve + Gelman, inviting pushback); the target venues; the correction that the text-as-data causal community are input-providers, not referees to simulate, and that the goal is a better idea, not a faked review; choosing the population estimand; the worked-example direction (prescriptivism, then "another language", then francophone feminization) and the taste calls (Croatian/Serbian in, German orthography out, feminization best); probing francophone Africa, then Swiss/Italian; the over-commitment check that produced the progressive build; the methods north star ("improve methods, not prove a finding"); the Gelmanian null-testing idea; recalling partial pooling; and the pacing/scope corrections that repeatedly pulled the assistant off its drift (toward proving a finding, faking a review, or over-committing).

ASSISTANT (Claude Opus 4.8): reviewer recommendations and the text-as-data framing; the five advisory personas and the simulated review boards plus their synthesis (a critique generator, NOT independent validation -- one model in five hats); the literature intake and the consequential find that Burnett & Bonami (2019) already ran the 1986/1998 case and hand over the ideology confounder; the date/corpus verifications; the f = pi + b framing and "the centrepiece's flaw is the thesis"; the design spec and the rung-1 prose; and the precise scoping of Brett's half-formed ideas (partial pooling -> shrink-within/fix-across + Hausman; null testing -> a diagnostic, though the assistant overstated it as inference at N=2). The policy-time-vs-calendar-time test and the France-1986-null placebo emerged from the simulated Roth/Cunningham personas (assistant-via-personas), independently in both the Opus and Codex boards.

CODEX (OpenAI): a redundant cross-model review board (round 1 and the v2 check) that validated the architecture and produced the "operationalize diagnostics into a decision-rule ladder" finding.

HONEST SUMMARY: the ideas that define the paper -- its question, its purpose, its pivots, its standards -- are Brett's; the LLMs supplied retrieval, structured self-critique, verification, and drafting. What exists is a developed plan, not a result: no real human-expert eyes and no real data yet.

2026-06-17 - Rung-1 fixes + decision ladder written (sections/08, passed check-style) and the ladder VALIDATED via simulation (simulations/rung1-ladder-sim.py; runnable, deterministic seed). Rung-1 fixes: randomization null downgraded to a falsification/calibration check (not "the right inference" at N=2); France reframed as a non-pristine control (1986 Fabius circular = near-null policy, by the government's own later admission), with the clean identifying window 1991-1997 and the 1998 Jospin shift held back as a follow-up event; the 1991 step reframed as possibly a waypoint, with a pre-1991 slope check; plus form-specific secondary estimate, interference estimand (direct vs total; AFP/French-media as exposure variables), and partial pooling kept off the event-time contrast and the editing layer. Spec (Section 3) gains the general "claim discipline / decision ladder" principle that every rung inherits. SIMULATION RESULT: all five controlled arms route to the predicted verdict -- benign -> bounded estimate (DiD +0.193); malign-rate (differential composition jump, no real effect) -> not identified/composition (DiD +0.197, a spurious effect nearly identical to benign's, correctly caught by the composition diagnostic = the paper's thesis in one row); interference -> not identified/interference; waypoint -> not identified/waypoint; null -> no effect/shared wave. Scope: validates the rule's self-consistency (routes controlled cases), the spine's first vertebra; still to add -- a recovery-under-reweighting arm (reweighting recovers pi, not just flags the artifact), the actual partial-pooling estimator (currently simple proportion means), a B&B-calibrated breakdown cutoff (thresholds are DGP-tuned, not derived), and a power check at realistic noise. Reason: validated the central fix before drafting rung 2; rung 2 now inherits a tested rule and is unblocked.

2026-06-17 - Cross-fertilization with Kinds_as_Projectibility_Profiles (Reynolds). That paper grades support for a kind claim as stabilizer -> maintainer -> controller and gives demotion rules (demote / localize / reject; "support-grade demotion" keeps the projection while downgrading the claimed support), under a profile specification (what it projects, under what scope, with what support, what stabilizes it, where it is demoted). OUR diagnostic-to-claim decision ladder is a domain-specific instance: "bounded estimate" = a graded, supported projection; "descriptive only" = support-grade demotion (the description stands, the causal support is downgraded); "shared wave, not policy" = scope/attribution localization; "not identified" = rejection. The Kinds paper already applies profile analysis to grammatical categories (deitality, passive), so corpus causal inference is a natural further instance, and Goodman (1955) is the shared projectibility root. DECISION: record the connection; keep it OUT of the methods body for the corpus / applied-linguistics audience (north star = methods, not the HPC programme); at most a single severable sentence in the conclusion at polish, citing the Kinds paper as the general support-grade/demotion account of which the ladder is an instance, and cut if it reads as grafting. Reason: the link is real and gives the ladder a principled backbone, but loading a methods paper with kind theory risks the audience and the north star; the more natural flow is the reverse (the ladder as a worked instance FOR the Kinds paper).

2026-06-17 - Rung 2 drafted (sections/08, passed check-style): scaling to staggered multi-variety (Québec 1979, Belgium 1993, with Suisse romande 1991 / France 1998). Estimator = Callaway-Sant'Anna group-time ATTs vs not-yet-treated controls; TWFE-fails diagnosis cites Goodman-Bacon + de Chaisemartin-D'Haultfoeuille; robustness Sun-Abraham / Borusyak et al.; sensitivity Rambachan-Roth; few-clusters BDM. Lesson = "more units is not more identification when the units are confounded or interfering": cross-country confounders, morphological-strategy divergence (Dawes), selection-into-treatment-timing (the deep cost), and few-clusters inference. France 1986-null = the internal placebo. Inherits the rung-1 decision ladder. Bib entries added to references-local.bib (CallawaySantAnna2021, GoodmanBacon2021, deChaisemartin2020, SunAbraham2021, BorusyakJaravelSpiess2024, RambachanRoth2023, Fujimura2005); vol/no/pages verified from the methods intake, most DOIs omitted pending verification. Reason: rung 2 inherits the validated ladder; the progressive build now has two rungs in prose.

2026-06-17 - Recovery simulation run (simulations/rung1-recovery-sim.py, stratified DGP, deterministic). PART 1 (load-bearing) VALIDATED: reweighting RECOVERS the true DiD under MEASURED composition drift (malign_rate: naive +0.128 spurious from a stratum-mix shift, reweighted -0.000 = true 0) and FAILS under UNMEASURED within-stratum drift (malign_unmeasured: naive and reweighted both +0.145; the residual "no unmeasured differential drift" assumption made visible as the case the method is honestly fooled by); benign recovers cleanly. So detect-AND-recover is now demonstrated, not just asserted. PART 2 (partial pooling): an honest NULL -- pooling gave no variance gain on the simple 2x2 because the pre/post averaging already pools across years; its payoff is deferred to rung 2+ (group-time ATTs over many sparse cells). FOLLOW-UPS: soften rung 1's partial-pooling line (it stabilizes sparse cells but does not tighten the 2x2 DiD) and relocate the pooling demonstration to rung 2. Reason: validate-before-assert paid off; the sim confirmed the recovery claim and corrected the assumption about where pooling helps.

2026-06-17 - Ran a full five-persona Codex-only review board on the drafted rungs 1-2 plus both simulation scripts (reviews/review-board-...-codex-rungs/), single model rather than the Opus+Codex redundant double-up, after the board-value assessment recommended dialing redundancy down. Reason: cross-model read on the actual drafted prose and the simulation evidence (not just the spec), at Brett's request for the full board.

2026-06-17 - Codex-only full board (5 seats) outcome on rungs 1-2 + both sims. Verdict UNANIMOUS 5/5 "draft the rest once the central fix is made"; direction endorsed. CENTRAL FINDING (5/5): the simulations are clean unit-tests / existence proofs, NOT power tests -- they validate the ladder routing and a post-stratification lemma "by construction" (the composition proxy directly encodes the jump; interference is passed in as the true bleed parameter; the measured/unmeasured split is binary), not whether the diagnostics fire under realistic noisy corpus data. "Validated" overstated it; the honest claim is they validate logic + a lemma, not empirical power (sharpens the pre-flagged no-power-check caveat). FIX (5/5): build a failure-map / operating-characteristics simulation -- noisy/partial proxies, a PARTIAL unmeasured-drift middle case, the strategy-mix arm the spec promised but the recovery sim lacks, interference combined with reweighting, treatment-by-stratum heterogeneity, sparse/zero cells, near-threshold cases -- and report MISROUTING RATES; then reframe the sims in prose as "sanity checks + failure maps." SPECIFIC: (a) DONE -- softened the rung-1 partial-pooling line (no 2x2 variance gain) and corrected the estimand overcorrection (need parallel trends in BOTH the population rate and the bias b, not "b not pi"; Roth); (b) rung 2's open bounded-estimate branch must either defend an exogeneity premise or be framed as "staggered DiD usually downgrades," and a selection-into-timing sim arm should be added (zero true effect, timing selected on ideology, event-time shows policy-aligned jumps -> ladder routes to not-identified); (c) narrow the France-1986 placebo to a one-mechanism negative control and source-verify 1986-vs-1998 differ by enforcement not corpus coverage; (d) mediator/collider warning -- reweighting on profession/referent/topic mix that is itself moved by policy removes part of the EFFECT, not just bias (Eisenstein); (e) align both sims to the acceleration/hazard estimand the spec promises (they test level jumps). BOARD-VALUE NOTE: a single-model full-seat board produced a sharp, unanimous, correct critique at ~half the redundant cost and reinforced the pre-flagged limit -- consistent with "keep the seats, drop the redundancy." Reason: the board correctly showed that clean validation overstates; the next step is the operating-characteristics / failure-map simulation.

2026-06-17 - Failure-map / operating-characteristics simulation built and run (simulations/rung-failure-map.py, deterministic, 400 reps/scenario), delivering the Codex board's central ask: MISROUTING RATES across realistic-mess dimensions, not five tidy successes. RESULTS: clean effect 0% FN; measured composition artifact caught 0% FP; HIDDEN drift outside the reweighting strata is uncatchable and false positives climb with the hidden fraction (56% -> 99% -> 100%); sparse cells destroy power (effect 0.2 at n=40 -> 94% FN); interference caught with clean exposure (0% FP) but noisy exposure leaks 8% FP; SELECTION->WAVE (a control on a different post-trajectory) is an irreducible 100% false positive in the 2-group design -- observationally identical to a real effect, which is exactly what rung 2's calendar-time test is for (Roth's point honestly rendered). So the simulations are now "sanity checks + failure maps," not "the pipeline works." STILL TO ADD: an explicit strategy-mix (form-detection-bias) arm (Dawes); a rung-2 staggered failure map where the calendar-time test actually CATCHES selection; and the prose follow-ups (reframe the section-08 sim discussion to cite misrouting numbers, the mediator/collider reweighting warning, alignment to an acceleration/hazard estimand). Reason: delivers the operating-characteristics layer the board demanded; the worked-example prose can now cite real misrouting rates instead of asserting the method works.

2026-06-17 - Section 08 prose reframed and small board fixes carried (passed check-style except a paragraph-length guideline on several pre-existing dense paragraphs, left for a polish pass). Added: a "What the simulations show, and where they break" subsection presenting the sims honestly as sanity checks + failure map, citing the misrouting numbers (measured composition caught; hidden-drift FP climbs past half to near-certainty; sparse cells miss real effects >9/10; selection->wave is the 2-group blind spot rung 2's calendar-time test exists to catch); the mediator/collider reweighting caution (reweighting only on margins the policy does not itself move, or it subtracts part of the effect); and the France-1986 narrowing (a one-date negative control, not a warrant for the other varieties' parallel trends). Reason: closes the Codex board's central ask and the small fixes; rungs 1-2 + the simulation discussion now hang together honestly.

2026-06-17 - Threat-structure DAG built into Section 5 (sections/05-identification-threats.tex; main.tex now loads tikz + arrows.meta + shapes.geometric in the project file, NOT the house-style symlink; compiles clean, 5-page PDF, passes check-style). A TikZ causal diagram with the four threat structures: the ideology common-cause fork (ideology -> policy timing AND ideology -> usage = selection-into-timing), the composition mediator (policy -> C -> f, so reweighting on C subtracts part of the effect), the measurement link (latent pi -> observed f), and neighbour spillover (SUTVA). Framed deliberately as a THREAT INVENTORY, not a DiD identification graph: the caption states that identification rests on parallel trends (a restriction on the time-trend of the ideology-induced confounding a static graph cannot display), so the design differences the confounder out rather than conditioning on it -- the caveat that keeps the figure from misstating DiD and from drawing the econometrics reviewers' fire. Consolidates several board points (Eisenstein's mediator/collider, Roth's selection-into-timing, the measurement layer, interference) into one figure. OPTION held in reserve: a full time-expanded two-period version that shows parallel trends visually. Reason: a targeted structural DAG earns its place for teaching the threat structure; a DAG-driven identification reframe would not.

2026-06-17 - First-person-plural purge + threat-DAG restructure. (1) PRONOUNS: replaced the editorial "we"/"our"/"us" throughout the drafted sections (08 worked example, 05 threat-DAG prose and caption) with first-person singular "I" or impersonal/passive constructions, after Brett gave a firm STANDING rule: never default to first-person plural in his single-authored papers. Saved as a durable feedback memory (applies to ALL his solo papers, not just this one), indexed in the project memory MEMORY.md. (2) DAG: at Brett's prompt, moved "Neighbour policy" to the top row beside Ideology and added an Ideology -> Neighbour-policy edge, with the spillover routed as a dashed arc over the top into Observed f. This makes the common-cause-across-polities structure explicit -- the same ideology drives every polity's adoption timing AND its usage -- which is exactly why a not-yet-treated neighbour is a contaminated control, tying the figure directly to the selection-into-timing finding from the failure map. Caption rewritten to say so. Compiles clean (6 pages); linter shows only soft "prefer" nits (natural contractions applied, formal-prose ones left; one "The X is Y" opener varied to an "I"-opener to drop the cadence count). Reason: the pronoun rule is a hard user requirement and had to propagate everywhere; the DAG change improves causal fidelity and links the figure to the paper's central identification threat.

2026-06-17 - Outcome section drafted + rung 3 + corpus-resource grounding, all from Brett's two questions (are there Hansard-like corpora for FR/CH? and why only lexical forms, not broader gender agreement?). (1) DAG nicety: Neighbour policy moved to the RIGHT of Ideology (arrow now reads left-to-right into it) and the spillover straightened to a short down-right edge into Observed f, replacing the over-the-top arc -- Brett's call on reading order. (2) §04 Outcome construction drafted from a stub: transparent (rule-reproducible) vs model-dependent (classifier/parser/embedding) outcomes and how each fails; the outcome is a BUNDLE of realization strategies (distinct feminine noun form autrice / feminine article-or-agreement on an epicene noun la ministre) that a single count wrongly treats as exchangeable, so decompose by strategy and test the assumption (Dawes 2003 on cross-variety strategy divergence); two refinements -- obligatory agreement (la table verte) sits at a ceiling and is noise not outcome, while the informative agreement is the contested feminine-article kind, which for epicene nouns is the ONLY signal; and a marker is meaningless until referent-conditioning is fixed (a feminine article = feminization only for an independently known-female referent). (3) Rung-1 outcome line (§08) sharpened from "I also report a form-specific estimate" to an explicit strategy decomposition carrying the epicene-only-signal point. (4) Rung 3 (§08) drafted as the design-CHOICE contrast: trilingual Switzerland holds polity + federal policy fixed while language varies (is a change language-internal/francophone-wave or polity-driven?), with the Swiss Federal Assembly Bulletin officiel as data home; the parliamentary register's lesson is that the official record is a normalized house-style transcription, so a transcription-convention change moves observed f with no change in usage -- the measurement link (pi -> f) of the Fig DAG becoming the dominant path -- plus speaker-pool drift as the women-MP share rises. (5) WEB-GROUNDED, not from memory (source-grounding law; corpus names = reference pointers): ParlaMint covers ~29 countries incl. France and Belgium but only ~2015 onward, so it MISSES the 1986/1991/1993/1998 shocks; Switzerland is NOT in ParlaMint; the Swiss Bulletin officiel is digitized 1891--1999, trilingual. Logged all three corpus resources (Bulletin officiel, ParlaMint, French Journal officiel/comptes rendus) to notes/source-verification.md with explicit TO-VERIFY items; prose mentions kept informal (no \citep) pending verification. Compiles clean (7 pp); lint down to 2 pre-existing formal-prose contraction nits, left intentionally. Reason: the two questions exposed a genuine outcome-definition refinement (bundle/strategy, ceiling, conditioning) and a rung-3 data home that doubles as the sharpest version of the paper's measurement threat.

2026-06-17 - Threat DAG revised after two Codex read-only evaluations (reviews/dag-eval-codex-*.md and dag-patch-codex-*.md). Codex's two substantive hits were internal-consistency problems between the figure and the paper's own text: (a) C was mislabelled a uniform "mediator" though most corpus-composition threats (archive coverage, outlet entry, author turnover, register/genre drift) are NOT policy-caused, which makes C a bias source/confounder (and a collider if reweighted on); (b) pi->f collapsed measurement, contradicting the paper's own f = pi + composition + measurement estimand and leaving the rung-3 transcription shock no home on the figure. Rejected its headline alternative (a full time-expanded two-period graph) as audience overload against the methods north star, but kept the level-vs-trend insight as one caption sentence. The patch (split C into a policy-moved mediator arm T->C and a non-policy composition source ->C; add a measurement node M->f) was then run back past Codex, which endorsed it and added ONE change: Ideology->M, because the same ideology wave can shift the transcription/coding convention, so measurement becomes another ideology-driven path to f that mimics an effect, and a parentless M understates the rung-3 danger; plus relabel the new node "Non-policy composition" (avoid the formal "confounder" overclaim) and caption it "adjust only on pre-specified non-policy margins, do not subtract." IMPLEMENTED: figure now 8 nodes / 11 edges -- Ideology (unobs), Policy timing T, Neighbour policy Tn, Pop rate pi (unobs), Composition C, Non-policy composition, Coding/transcription M, Observed f; edges I->{T, pi, Tn, M}, T->pi (effect), T->C (mediator), Non-policy->C, {pi, C, M}->f, Tn->f (dashed spillover, shorthand for via-pi/C/M). Caption rewritten: f is produced not revealed; C's two sources call for opposite treatment; ideology as a moving wave drives timing, usage, AND the convention; fixed ideological level differenced away, trend is not. Compiles clean (7 pp); lint clear after one caption contraction. Source-grounding: SWIG (Richardson & Robins) has NO local bib key (verify before any SWIG citation); Dawes2003 still carries a page-range note. Reason: both hits were genuine inconsistencies between the figure and the paper's estimand/prose; the patch fixes them with one node-pair plus one Codex-suggested edge, without tipping into a DiD tutorial.

2026-06-17 - Methods spine started; §02 (The DiD estimand in corpus terms) drafted from a stub (Brett chose "methods spine, §02 first" over finishing §08 / source pass / abstract, via the next-step fork). §02 is the keystone the worked example already silently used. Load-bearing choices it now fixes for the rest of the paper: (1) the estimand is the ATT on the LATENT rate pi, not on the observed frequency f; (2) the central decomposition f = pi + b stated formally, b bundling composition + measurement (the C and M channels of the §05 DAG); (3) because differencing is linear, DiD(f) = DiD(pi) + DiD(b), so PARALLEL TRENDS IS NEEDED TWICE -- in pi AND in b -- and the section explicitly kills both overcorrections (treating composition as the whole problem and forgetting the rate; vs adjusting for composition and assuming the rate takes care of itself), per the board's earlier catch that it's "both, not b-not-pi"; (4) the FOUR-UNIT taxonomy named as \term{}s: linguistic unit (the variable whose rate is at issue), measurement unit (what gets counted: tokens/documents), identifying unit (where treatment varies: variety/polity), clustering unit (where SEs are taken). Warning that millions of measurement units coexist with 2-3 identifying units and only the latter govern conclusions; counting tokens as N manufactures false precision (BDM2004). Added \label{sec:estimand/diagnostics/worked}; builds clean (8 pp), lint clean. Foundational-DiD citation left as a % TODO (Card-Krueger 1994 or Roth et al. 2023 review) to add only after verification + keying -- not fabricated. Reason: the worked example leaned on all four of these without any prior section establishing them; §02 now carries the definitional weight §08 was improperly holding.

2026-06-17 - Codex-only 5-seat board (Keith/Roth/Cunningham/Gelman/Eisenstein) on the integrated draft (§02/04/05/08; stubs flagged), folding in the three author questions. Outputs in reviews/review-board-20260617-183403-codex-spine/. VERDICT unanimous 5/5 "Sound-with-revisions"; spine endorsed, no rethink. The three questions came back unanimous and were ACTED ON: (Q1) add AGGREGATION as a distinct FIFTH unit, do NOT fold into clustering -- aggregation = the cell counts are summed into before estimation (outlet-month, title-variety-year), setting weights/denominators and able to change the estimand; clustering = dependence structure for SEs. So my earlier instinct (fold into clustering) was wrong. DONE: §02 now lists five units. (Q2) KEEP f = pi + b but define b := f - pi as an exact accounting identity on the frequency scale, not a homoskedastic error model -- the linear split DiD(f)=DiD(pi)+DiD(b) is then exact by construction; only the interpretation is scale-dependent (doesn't transfer free to log-odds). DONE: added that sentence to §02. (Q3) cite the Roth/Sant'Anna/Bilinski/POE 2023 review (NOT Card-Krueger, which pulls toward example-led mental model), in §02 after the ATT/parallel-trends sentence; all flagged it needs verification. DONE: VERIFIED via ScienceDirect/RePEc/arXiv (J. Econometrics 235(2):2218-2244, doi 10.1016/j.jeconom.2023.03.008; note the 4th author is John POE, reviewers misspelled "Poet"), keyed as RothSantAnnaBilinskiPoe2023 in references-local.bib, cited in §02. CONSENSUS DESIGN WEAKNESSES (carried forward): (1) TOP -- the target population for pi drifts between sections (§02 "community" vs §08 "edited written standard"); must be fixed early because it decides whether composition is nuisance vs target (Keith/Cunningham/Gelman). PARTLY DONE: §02 now says the population is part of the estimand and must be fixed early; the one-sentence rung-1 estimand in §08 is the next edit. (2) diagnostics OVERCLAIM -- "checked/licensed" should be falsify/bound/never-confirm; bounded estimate = sensitivity-framed, not diagnostic-licensed (Roth/Gelman/Eisenstein). PARTLY DONE: softened the §02 diagnostics line + added "flat pre-trends can refute, never confirm"; §08 ladder wording still to soften. (3) treatment does too much / clean-step-shock vs dynamic diffusion -- §03 must define the treatment as the specific policy event, and the estimand is plausibly an acceleration/hazard not a level shift (Gelman/Eisenstein) -> mandate for §03. (4) diagnostics asserted not operationalized (AFP exposure, slope checks, randomization nulls, "breakdown larger than plausible ideology shift", which margins are reweightable pre-commitment) -> mandate for §06; sims need a visible DGP skeleton. (5) Keith: operationalize the text-as-measurement vs text-as-confounder boundary (the "Madame" cue may itself vary by register/outlet/period); add a role table -> §05/§06. (6) Gelman's standing challenge: why DiD not a multilevel model of the whole series -> needs an explicit answer paragraph in §07. SELF-CHECK: the Q1/Q2/Q3 unanimity is genuine (Q2 is just algebra), not artificial smoothing, and the five design weaknesses are lens-differentiated (Keith=measurement boundary, Eisenstein=diffusion/dependence, Gelman=multilevel, Roth=diagnostics-can't-verify, Cunningham=one-estimand-sentence), so the board is not collapsing to a centroid. CAVEAT noted to Brett: Q3 recommends a review co-authored by reviewer Roth -- mild self-recommendation, though all five converged on it. Reason: board confirmed the spine and produced a clean, mostly-mechanical fix list; the cheap §02 fixes + the verified anchor are done, the rest are now explicit mandates for the stub sections I'm drafting.

2026-06-17 - Two MORE external reviews (pasted by Brett, strong/detailed) triangulated with the Codex board. VERDICT both: strong draft, real organizing idea, but WRONG CENTRE OF GRAVITY -- §8 is doing the work §1-7 should; redistribute the machinery (esp. move the decision ladder into §6 FIRST, then instantiate in §8). CAUGHT AN ERROR I INTRODUCED THIS TURN: the §02 sentence "flat pre-treatment trends can refute them, never confirm them" is backwards (flat pre-trends are CONSISTENT with PT, they don't refute it; NON-parallel pre-trends discredit it). FIXED to "pre-treatment comparisons can discredit them but cannot verify them" (reviewer-suggested). SURGICAL FIXES MADE THIS TURN (all high-confidence, already-drafted prose): (a) §02 pre-trends sentence corrected; (b) §02 four->five scale-count inconsistency fixed (both reviewers caught it); (c) §02 added R2's three-way decomposition -- the bias term reaches the contrast as a non-policy drift (nuisance, must be parallel) AND a policy-induced shift (a mediator, part of the effect), which resolves the apparent tension between "DiD of b is contamination" and "reweighting on a policy-moved margin subtracts the effect"; coheres with the §05 DAG split; (d) §02 added a functional-form caveat that additive-scale PT is a commitment that bites when baselines differ (-> §07); (e) §08 rung2 stated plainly that heterogeneity-robust estimators (CS/SA/BJS) fix the WEIGHTING contaminant only and touch NOTHING of selection-into-timing; (f) §08 corrected the calendar-time test OVER-CREDIT (R1's highest-confidence gap): the test separates one SYNCHRONIZED wave from policy-locked timing but cannot distinguish a policy effect from HETEROGENEOUS LOCAL waves that each drive their own polity's date, so rung 2 is a partial filter not a rescue, and for this phenomenon the honest modal verdict is descriptive-or-not-identified (reaffirms north star). CHECKS: the run-together "typos" R2 saw (contendwith, samewire, standardwarning, aweighted) are NOT in source (source has proper spaces) and pdftotext extracts them spaced -- they're the reviewer's PDF copy-paste artifacts, our XeLaTeX text layer is clean; NOT a bug. The "8 WORKED EXAMPLE header on page 3" IS plausibly real (oversized Fig 1 [t] float + the known \headheight-too-small fancyhdr warning); fix folded into the §5 expansion (move the ~250-word caption into §5 body, shrink the float). MANDATES NOW EXPLICIT for the stubs: §6 = generalize the decision ladder + operationalize diagnostics (BINDING -- §6 is forward-referenced as the thing doing the epistemic work and is empty, so it jumps the dependency queue); §7 = "Estimation and inference choices" incl. wild cluster bootstrap (Cameron-Gelbach-Miller), randomization inference (MacKinnon-Webb), additive-vs-logit estimand, TWFE warning, analysis on pre-specified cells, AND Gelman's why-not-multilevel answer; §3 = treatment-as-history (announce/enforce/uptake/dose/anticipation/spillover); §5 = full threat taxonomy + absorb the caption; §1 = motivate for corpus linguists; §4 = per-period/variety classifier eval + a denominator paragraph; abstract = foreground the contribution (corpus DiD identifies only after target population/composition/measurement/identifying/clustering units are separated), example = stress test not payoff. Rung-2 not-yet-treated asymmetry (France 1998 has no within-set clean control) and rung-3 "not a clean DiD, a triple-difference / measurement-exposing variant" to be written in as lessons. Adoption dates + new §7 cites logged to notes/source-verification.md for source-checking before keying. Build clean (8 pp), lint clean but for 2 pre-existing formal-prose contraction nits. Reason: the external reviews confirmed the spine and the redistribute-the-machinery plan, caught one real logic error and one count inconsistency I had just introduced, and sharpened three already-drafted passages; the binding next task is drafting §6 and moving the ladder there.

2026-06-17 - §6 (Diagnostics) drafted and the decision ladder MOVED from §08 Rung 1 into §6 (the binding gap all three review sources named: §6 was empty but forward-referenced as the thing doing the epistemic work). §6 now contains: (1) the STANCE -- a diagnostic can disqualify or bound, never confirm parallel trends; diagnostics are pre-registered tests, not a post-hoc blessing; (2) Keith's ROLE assignment up front -- each textual feature is exactly one of measure-outcome / identify-referent / index-composition / mediator, and no feature can hold two jobs (conditioning on a mediator subtracts effect, on a drifting cue imports drift, reweighting a policy-moved margin removes signal); (3) five operationalized diagnostic families, each tied to a §5 threat and a verdict-move: pre-period/event-study + onset-vs-acceleration (SunAbraham, BorusyakJaravelSpiess, RothSantAnnaBilinskiPoe on flat-pre-trends-can't-prove-PT); placebo/near-null/label-permutation as CALIBRATION not inference with few clusters (BDM, handing few-cluster inference to §7); leave-one-source-out; composition reweighting on PRE-COMMITTED non-policy margins + outcome-strategy decomposition (§4); sensitivity/breakdown vs the largest plausible confound (RambachanRoth); (4) the DECISION LADDER as the general pre-specified rule (bounded/descriptive/shared-wave/not-identified) with the routing fixed before the estimate is seen and "bounded ... scoped to the register actually measured" answering the target-population weakness; (5) closing: a downgrade is a result. §08 Rung 1: the general itemized ladder REMOVED (0 itemize blocks left in §08; 1 now in §06), replaced by a compact case-specific application that runs the §6 ladder on the Swiss/French case and points to \ref{sec:diagnostics}; the "apparatus in miniature" sentence retargeted to "the §6 ladder applied." Added \label{sec:outcomes} (§4) and \label{sec:threats} (§5) for the new cross-refs. Build clean, all refs resolve, 10 pp (was 8); §6 lints clean, §08 only the 2 standing formal-prose contraction nits. Cites all from references-local.bib (no new keys). STILL OPEN for §6's neighbours: §7 (few-cluster inference toolkit + functional form + multilevel answer), §3 (treatment-as-history), §5 expansion (absorb the Fig-1 caption, fixes the float/header bug), §1, §4 additions, abstract. Reason: §6 is the section that makes the paper's epistemic stance real rather than asserted; with the ladder generalized here, §08 reads as an instantiation as the reviewers asked.

2026-06-17 - §7 drafted and RETITLED "Estimation and inference choices" (R2's suggestion; the \label stays sec:modern so the §02 functional-form forward-ref still resolves). Covers the three review mandates plus Gelman's challenge: (1) ESTIMATOR -- TWFE is a trap under staggered adoption (negative weights; GoodmanBacon, deChaisemartin), group-time/event-study estimators (CS/SA/BJS) repair the WEIGHTING ONLY and touch nothing of selection-into-timing ("a clean number for the wrong quantity"); (2) SCALE -- additive f=pi+b buys linearity but parallel trends isn't transform-invariant and the strain is worst at different baselines (RothSantAnna2023 functional-form), logit PT may be more defensible but then the additive decomposition doesn't carry through, so the scale is a substantive choice; binomial/beta-binomial fit cell rates without changing identification, report back on the claim's scale; (3) INFERENCE -- binding N is identifying units not tokens, cluster-robust intervals anti-conservative with 4-5 polities (BDM), the toolkit is wild cluster bootstrap (CameronGelbachMiller2008) + randomization inference for few treated clusters (MacKinnonWebb2020), and "no clean interval" is a verdict reached AFTER running them, not instead; analysis on pre-specified cells not raw tokens (the §2 aggregation unit); (4) GELMAN'S why-not-multilevel answered at the level of identification vs estimation -- DiD puts parallel trends on the table where a pre-period can discredit it and differences the confound out, whereas a full series model folds the identifying assumption into functional form + RE structure unchecked; partial pooling is a COMPLEMENT for the nuisance dimensions (shrink sparse outlet/title cells within varieties, across-variety contrast fixed), not a substitute for the design. THREE NEW CITES verified via MIT Press/RePEc, ScienceDirect/RePEc, Econometrica/Wiley and keyed in references-local.bib: CameronGelbachMiller2008 (ReStat 90(3):414-427, doi 10.1162/rest.90.3.414), MacKinnonWebb2020 (J.Econometrics 218(2):435-450; DOI omitted -- search only inferred it, matching the bib's modern-DiD no-DOI style), RothSantAnna2023 (Econometrica 91(2):737-747, doi 10.3982/ECTA19402; distinct from RambachanRoth2023 and the RothSantAnnaBilinskiPoe2023 review). Build clean, all cites resolve, 10 pp, §7 lints clean. STILL OPEN: §3 (treatment-as-history), §5 expansion (absorb Fig-1 caption -> fixes float/header bug), §1, §4 additions, §9 conclusion, abstract; plus the source-verification queue (adoption dates). Reason: §6 forward-referenced §7 twice (few-cluster inference, functional-form scale) and §7 was the convergence point of three review mandates; the methods spine is now §2/§4/§5/§6/§7 drafted, leaving the framing sections (§1/§3/§9 + abstract) and the §5 expansion.

2026-06-17 - Figure 1 (threat DAG) re-laid-out to 3 clean rows at Brett's request (was scattered across y=1.8/2.4/3.0 up top + 0.2/0.4 and -1.4/-1.6 misalignments). New grid: TOP row (y=1.8) = Ideology + the children that reach f WITHOUT passing through pi (Neighbour policy, Coding/transcription); MIDDLE row (y=0) = the causal spine Policy timing -> pi -> f; BOTTOM row (y=-1.8) = the composition layer (Non-policy composition -> Composition C). pi and C vertically aligned at x=4. The I->M edge (ideology drives the transcription convention) is routed as a bend-left=30 arc over the top so it clears Neighbour policy. Same nodes/edges/semantics as before, just aligned; caption unchanged (still accurate). Compiles clean, no tikz errors. NOTE: coordinates tuned by reasoning, not eye -- two spots to eyeball on /view: the I->M arc clearing Neighbour policy, and the convergence of four edges (pi, C, M, dashed Tn) at f.

2026-06-17 - §3 (What counts as treatment in language data) drafted from stub, the "treatment-as-history" section both external reviewers said was doing real work scattered through Rung 1. Thesis: in language data the treatment is rarely a single dated act, and WHICH act you call the treatment is itself an identification decision that fixes the estimand. Covers: (a) the menu of candidate treatments for one policy (endorsement / dictionary [Moreau1991] / circular / editorial uptake), differing in date and force; (b) announcement vs implementation -- France 1986 unenforced vs 1998 enforced (BurnettBonami2019), which is what makes 1986 a near-null treatment usable as a negative control, not a failure; (c) anticipation/actuation -- an endorsement often ratifies change already under way (BurnettBonami2019 on actuation), so the date is a waypoint not an onset -> define the treatment as a change in the adoption RATE, ties to the §6 pre-period diagnostic; (d) dose/intensity -- treatments aren't binary (scope, teeth), prefer an intensity to an on-off indicator where measurable; (e) the treatment boundary LEAKS across units via shared media (-> §5 interference). Close: the first question of a corpus DiD is which act/date/reach is the cause; leaving it implicit is how a study defends a number whose meaning it never fixed. NO new cites (Moreau1991, BurnettBonami2019 only, both keyed + grounded); deliberately did NOT attribute a specific empirical finding to Fujimura2005 (haven't read it) -- used BurnettBonami for actuation, which is that paper's actual topic. Build clean, 11 pp, §3 lints clean. Reason: §3 consolidates the fuzzy "language policy as treatment" problem the reviewers called half the identification story; spine now §2/§3/§4/§5/§6/§7/§8 drafted, leaving §1, §9, abstract, and the §5 prose expansion (absorb the caption).

2026-06-17 - §1 (The problem) + abstract drafted (shipped the prior checkpoint first at 424cdd0 per Brett's "after shipping"). §1 is the corpus-linguist on-ramp the reviewers wanted BEFORE the estimand machinery: opens on the concrete trap (a form rises after a shock -> "the shock changed the language"), names DiD as the standard repair while explicitly NOT claiming it's new to language research (interrupted time series / before-after are old here; per the project CLAUDE.md caution), locates the contribution as TRANSLATION/hygiene not a tutorial, states the five-things-to-separate (target population / corpus composition / measurement procedure / identifying unit / clustering unit), previews the SIX questions mapped to sections (treatment §3, identifying unit §2, target+outcome §2/§4, comparison+corpus-threat §5, diagnostic §6, estimation/inference §7), and frames the worked example as a teaching vehicle whose honest verdict is sometimes not-identified (north star). Avoided the AI-tic scene-setting/authority-quote opening. ABSTRACT (main.tex, replacing the TODO) foregrounds the CONTRIBUTION not the feminization example, per R2: before/after frequency contrasts -> DiD correction -> assumptions written for panels not corpora -> f = rate + composition/measurement -> separate five units -> parallel trends twice -> diagnostics disqualify/bound never confirm -> the decision ladder -> worked example as stress test not finding. ~165 words; keywords already synced. Added \label{sec:problem}. Build clean, 12 pp, both lint clean. REMAINING: §9 (conclusion -- the methodological moral: corpus DiD succeeds when it disciplines a claim downward), the §5 prose expansion (absorb the ~250-word Fig-1 caption, should clear the float/header bug), and the source-verification queue (adoption dates). Reason: with §1 and the abstract the paper now reads front-to-back except the conclusion; only §9 + the §5 rebalance remain before a full proofreading/verification pass.

2026-06-17 - De-strawmanned the abstract and §1 openings (Brett flagged the abstract's "Corpus linguists routinely read a rise ... as evidence that the shock changed the language" as a strawman; directive: follow Rapoport's rules, don't strawman the audience). The line had three faults: (a) "routinely ... as evidence that" asserts an EXPLICIT causal syllogism as the field norm when in practice the causal reading enters IMPLICITLY, via verbs ("responds to", "reflects", "driven by"); (b) "routinely" is an unsupported empirical claim about practice (no cite, violates source-grounding); (c) it aims at the naive end of a field whose careful end (variationist actuation work) is the actual IJCL/Corpora/CLLT readership, and the adversarial reviewer would pounce. FIX (Rapoport register): abstract now opens "...whether the shock caused the change is a natural question to ask. Difference-in-differences is built for that counterfactual and HAS BEEN USED in language research..." -- legitimate question + credit to prior use, gap located in the corpus confounds (composition/measurement), no neglect claim. §1 para 1 reworked to CREDIT the field ("corpus linguistics is well equipped to ask it ... the field has long studied how change actually propagates"), characterize the causal slide accurately as implicit-via-verb, and frame the gap as "not because linguists neglect it but because a corpus adds confounds the standard designs were never built to handle." Build clean, both lint clean. Reason: the strawman opening would have alienated the exact sophisticated audience the paper targets and rested on an unsupported "routinely"; the charitable framing is also more accurate (the causal reading is implicit, not asserted) and foregrounds the real contribution faster.

2026-06-17 - De-myopia'd the opening example lists + added a calibrated corpus-availability acknowledgment (Brett: the original list, "spelling reform, style guide, platform feature, law", is all modern bureaucratic interventions presupposing a standardized language; "quite a myopic view"). FIX: broadened the example lists in BOTH §1 and the abstract to span kinds of shock, contact/migration ("speakers of another language arrive"), media/technology ("a new medium spreads"), sociopolitical ("a border is redrawn", which also echoes the design's Croatian/Serbian case), and institutional ("a government decree") -- only one of four now bureaucratic. ADDED to §1 a measured paragraph naming the deeper point: the DiD method is language-general but the corpus DATA is not, which is the paper's own composition problem at the macro scale (which languages get a corpus at all). CALIBRATION per Brett's steer "don't overplay it -- we have reasonably large pre-modern corpora for many European languages, Chinese, and a few others": the acknowledgment says coverage is skewed "but not narrowly", names the historical breadth (European languages, Chinese, others), so the before/after series reach "well past the modern standardized case", then keeps the honest limit ("where none exists the design falls silent") and ties it to composition. Reworded its opener from "The method is general; the corpus data is less so" to "Corpus coverage lags the method's reach..." to clear a 3-in-a-row "The X is Y" paragraph-opener cadence flag. Build clean, lint clean. Reason: the bureaucratic example list tracked a real (but bounded) selection in what corpus data exists; broadening the examples + a non-inflated acknowledgment turns the vulnerability into a point and connects it to the paper's composition spine, without overclaiming the data gap.

2026-06-17 - §9 (Conclusion) drafted, completing the full draft (all of §1-§9 + abstract now written). Per both external reviewers, it makes the NORMATIVE moral rather than summarizing: (1) the one refusal -- don't read a corpus frequency as a population rate -- from which the whole discipline follows; (2) THE MORAL: a corpus DiD earns its keep when it pushes a causal claim DOWNWARD; "shared calendar-time shift / measurement shock / composition artifact / not identified" are first-class ANSWERS, not failures, and reaching one counts as success; (3) WHEN before/after corpus data CAN carry a causal claim -- named treatment at a named date, a counterfactual-making (not decorative) comparison, composition/measurement held on pre-committed margins or shown static, and a sensitivity bound beyond any plausible confound -> a bounded estimate scoped to the measured register, not a fact about the language at large; (4) WHEN it CANNOT (the more common honest output) -- few identifying units, selection-into-timing, unseen drift -> not a defect but the method reporting what the data supports; (5) close: the feminization case was a place to watch the apparatus work and break, not a finding about French; the apparatus travels to other shocks/languages "as far as the corpora reach" (ties back to the §1 data caveat); ends on knowing, before publishing, which claims are worth trusting -- no optimistic-vague AI close. No new cites. Build clean, 13 pp, lint clean. REMAINING before a full proofread/verification pass: the §5 prose rebalance (absorb the ~250-word Fig-1 caption into the section body, should clear the float/header bug) and the source-verification of the adoption dates (Burnett & Bonami + primary docs) and the Dawes page range. Reason: §9 delivers the methodological-hygiene moral the title/subtitle promise and the reviewers asked for; the paper now reads complete end-to-end, leaving only the §5 rebalance and source verification before external eyes.

2026-06-17 - §1 citations added AND the novelty claim de-overclaimed (Brett, two coupled messages: "section 1 lacks citations" + challenging "what's missing is an account of what [DiD's] assumptions become when the data is a corpus" -- "Is this truly missing?"). HONEST ANSWER: no, not truly missing -- causal inference FROM text is an active field (Grimmer/Roberts/Stewart 2022 Text as Data; Egami et al. 2022 Sci Adv), so a bald "no account exists" overclaims and the board's Keith seat would flag it. The DEFENSIBLE narrower gap: that literature mostly treats text as treatment/outcome/confounder CROSS-SECTIONALLY; what's unsettled is DiD's own parallel-trends + no-interference assumptions re-expressed for a DIACHRONIC corpus frequency f = pi + b (parallel trends twice; interference via shared media). FIX = reframe + the missing cites, same edit: §1 para 2 now credits the text-causal field and narrows the claim to "carrying [DiD's assumptions] across is the gap this paper works in." CITATIONS added (source-grounded, only verified): from central bib -- weinreich1968 (Weinreich/Labov/Herzog, actuation) + tagliamontedarcy2007 (tracking change in progress) for "the field has long studied how change propagates"; newly verified + keyed in references-local.bib -- biber1993 (Representativeness in Corpus Design, LLC 8(4):243-257) for "a corpus is a sample of texts assembled with priorities"; egami2022texts (Sci Adv 8(42):eabg2652, doi 10.1126/sciadv.abg2652) + grimmer2022text (Text as Data, Princeton UP 2022) for the text-causal field. NOTE: could NOT verify Keith/Jensen/O'Connor 2020 via web this pass (search returned junk), so did NOT cite it from memory -- anchored on the two that verified. Also dropped the abstract's unsupported "and has been used in language research" (no verified DiD-in-linguistics instance to cite; abstracts don't carry cites). Removed the redundant "That's the gap this paper addresses" opener from §1 para 3 (para 2 now states the gap). Build clean, all 5 cites resolve in bbl, lint clean. FOLLOW-UP: the text-causal cites (Egami/Grimmer) should probably also appear in §5 where the composition-as-confounder threat lives (the board's Keith point: build on text-as-confounder work, don't reinvent); Keith/Jensen/O'Connor 2020 to be verified and added when reachable. Reason: the citation gap and the novelty overclaim were one problem; crediting the existing text-causal literature both grounds §1 and makes the contribution claim honest (a translation, not a void).

2026-06-17 - §5 rebalanced from one-paragraph-plus-giant-caption into a proper THREAT TAXONOMY (the last structural reviewer item). Both external reviewers: §5 was a single paragraph while Figure 1's caption ran ~250 words carrying body-text argument; move the caption prose into the section and leave the caption to identify nodes + the warning. DONE: caption shrunk to ~85 words (dashed = unobserved; the arrows named as "the routes the section takes up in turn"; the warning that parallel trends is a trend a static graph can't draw). Body now opens by naming FIVE threat families, four corpus-inflected (composition, measurement, interference, aggregation) + the fifth, selection-into-timing, as the ordinary DiD confound, each a way the observed contrast moves while pi stays put, each tied to a §6 diagnostic. One worked paragraph per family: composition (mediator T->C vs non-policy drift, opposite handling, name the reweight margins in advance); measurement (coding/transcription drift, f jumps with pi fixed, "confounding read off the text itself" with keith2020text as the anchor -- the board's Keith point about building on text-as-confounder work rather than reinventing); interference (shared wire/media, control "lightly dosed", name direct-vs-total estimand); aggregation (token/cell level fixes weights+denominators, can move the estimand with no usage change, the §2 aggregation unit); selection-into-timing (the deepest, ideology fork = common cause of timing and usage, no estimator removes it, observationally indistinguishable from an effect in 2-group). Close moves the parallel-trends point into the body: the figure draws a static fork but identification turns on a trend, so the diagnostics of §6 (not the diagram) carry the weight. keith2020text now cited here too. tikzpicture preserved verbatim. LAYOUT BUG FIXED: the running header "8 WORKED EXAMPLE" that R2 saw on page 3 (oversized Fig-1 float drifting) now appears only on pages 9-11 where §8 actually is -- the expanded §5 keeps the figure on its own pages. Build clean, 15 pp, §5 lints clean. Reason: §5 is now the threat inventory the reviewers asked for and Figure 1 reads as earned rather than dropped in; with this the full redistribute-the-machinery program (ladder->§6, estimators->§7, treatment-history->§3, threat-taxonomy->§5) is complete and the draft is structurally done end-to-end. REMAINING: source-verification of the adoption dates (Burnett & Bonami + primary docs) and the Dawes page range, then a full /proofread before external eyes.

2026-06-17 - Source-verification pass run (after shipping ab9e16a). ALL five worked-example adoption dates VERIFIED against authoritative/primary sources by RETRIEVING them (not memory): Québec 1979 (OQLF first avis de recommandation), Suisse romande 1991 (Federal Chancellery non-sexist-formulation report, June 1991, trilingual + Moreau1991 dictionary), Belgium 1993 (Communauté française décret du 21 juin 1993; axl.cefan/ULaval + cfwb.be), France 1986 (Fabius circulaire du 11 mars 1986; Légifrance JORFTEXT000000866501), France 1998 (Jospin circulaire du 6 mars 1998; Légifrance JORFTEXT000000556183). EVERY DATE IN THE PROSE IS CORRECT -- no corrections needed. Bonus: R1's worry that France-1986 is "tight against Fabius's March 1986 departure" RESOLVES IN THE PAPER'S FAVOUR -- the government fell days after the 11 March circular (16 March elections), which is exactly why it went unenforced, strengthening the near-null/negative-control framing. DAWES2003 page range + DOI RESOLVED: Ethnologies 25(2):195-213, doi 10.7202/008054ar (Érudit); pages+doi added to the bib entry, "verify before polish" note removed (0 verify-notes left in references-local.bib). notes/source-verification.md updated with the verified findings. STILL OPEN (corpus-DATA items, not dates, not blockers for a methods paper): the Swiss Bulletin officiel exact digitized coverage + the dated transcription-convention change (rung 3's central confound), and whether a clean French comptes rendus corpus exists for 1986-1998 -- these matter only if the example is actually run. OPTIONAL polish: primary-source \citep for Québec/Belgium/Switzerland instruments (France already covered by Légifrance + BurnettBonami2019). Build clean, Dawes resolves with pages. Reason: the load-bearing empirical claims are now source-grounded per the LAW; the draft is structurally complete AND date-verified, so the next gate is a full /proofread before external eyes.

2026-06-17 - Applied "the two points": (1) PRIMARY-SOURCE CITATIONS added for all five worked-example instruments (oqlf1979 Gazette officielle du Québec 111(30):7394-7395 1979; belgique1993decret décret 21 juin 1993; chancellerie1991 Federal Chancellery 1991 Berne; france1986circ/france1998circ Légifrance), cited once each at first mention (§3 France, §8r1 Switzerland, §8r2 Québec+Belgium); France keeps BurnettBonami2019 as secondary. (2) BULLETIN OFFICIEL firmed: published 1891, digitized, but FULL VERBATIM debates only from 1971 (referendum-decree proceedings only before) -- rung-3 prose updated to "digitized from 1891 and carrying full verbatim debates from 1971"; the 1991 window is safely inside. Build clean, all 5 cites resolve.

2026-06-17 - NON-REDUNDANT MIXED Codex+Opus full board run on the complete draft (reviews/review-board-20260617-201033-mixed-codex/, SYNTHESIS.md there). Opus: Keith/Gelman/Eisenstein (all MINOR revisions). Codex: Roth/Cunningham (both MAJOR revisions) -- a real cross-model SEVERITY split on the same substance (Codex treats the estimand ambiguity as a blocker; Opus as fixable-without-re-architecting), preserved not collapsed. CROSS-MODEL CONSENSUS: (1) LOCK THE ESTIMAND -- §2's three-way decomposition says target = effect on population rate pi BUT also says the policy-moved filter shift (T->C->f) is "part of what the policy did", and Roth+Cunningham+Keith say that's a DIFFERENT (edited-output) estimand; if target is pi, T->C->f is not part of it. Remedy = pick one + a per-rung ESTIMAND LEDGER (target/treatment/control/window/outcome/identifying-unit/clustering-unit/direct-vs-total); pin the pi population denominator (it drifts across rungs). This is the board's central finding. (2) GROUND THE SIM NUMBERS -- UNANIMOUS 5/5: §8.3's +0.13 / "past half" / "nine in ten" appear with no DGP/table/code pointer, a violation of the paper's OWN source-grounding rule (sims exist in simulations/ but the reader can't see them); add a table/appendix (ideally Quarto) and kill the §8 "simulation spine" TODO. (3) operationalize the ladder thresholds (Roth); (4) temper §7 inference remedies -- WCB/RI aren't generic fixes for 4-5 selected clusters (Roth). DISTINCTIVE: Gelman -- the §7 "why not multilevel" para OVERSTATES (paper itself says no estimator removes selection-into-timing, so DiD doesn't difference the trend confound away; rest the case on transparency/convention) + the second PT assumption (in b) is untestable since pi is latent. Eisenstein -- feminization is gradual logistic diffusion, so "not identified" is modal BY CONSTRUCTION; state step-change is a precondition on the CHANGE not just the policy; add leave-one-author-out + per-strategy relative timing as diagnostics. Keith -- outcome/confounder non-separability (Madame cue tracks composition). Cunningham -- rung 3 not as executable as 1-2; complete or label extension. NON-ISSUE: both Codex seats false-flagged weinreich1968/tagliamontedarcy2007 as "not in references-local.bib" -- they're in the CENTRAL bib and resolve (Codex only saw the local bib). Reason: the mixed board confirmed the spine (all 5) and surfaced one genuine conceptual gap (estimand lock) + one unanimous grounding gap (sim numbers) as the two must-fix items before external eyes; full prioritized list in SYNTHESIS.md.

2026-06-17 - ESTIMAND LOCK done (board fix #1; touches §2/§5/§6/§8). Resolved the usage-vs-output ambiguity the board flagged by making pi (the USAGE RATE) the PRIMARY estimand, with a pinned denominator (Keith): "the share of profession- and title-noun tokens with a female referent that take feminine marking, in a named text population." Key REVERSAL of an earlier (Eisenstein-DAG-round) framing: policy-moved composition is NOT a "mediator to keep / don't subtract" for the usage estimand -- it's NUISANCE to reweight away (the policy changing what gets written, not how a referent is marked, is not a usage effect). The "part of the effect, don't subtract" reading is now correctly attributed to a DIFFERENT, total-output estimand, and the paper says the two estimands "differ exactly by the policy-to-composition path" and "a design that doesn't say which it means can defend neither." The ONLY margin neither estimand conditions on is the outcome's own realization strategy (downstream/collider). Added Gelman+Roth's offsetting point: since only f is observed, a flat pre-period in f tests PT-in-pi and PT-in-b JOINTLY, never apart (they can cancel). Added a per-rung ESTIMAND LEDGER table (Table 1, tab:ledger, in §8: target/treatment/control/window/outcome/identifying-unit/clustering-unit/effect-type) -- Cunningham's ask; it also makes rung 3's "not a clean DiD, exposes the measurement link" status explicit. Propagated the reframe to ALL old-framing sites (grep-verified none left): §5 composition threat (mediator-vs-confound now estimand-dependent), §6 roles paragraph (the mediator-you-mustn't-touch is the realization strategy, not policy-moved composition) and §6 fourth diagnostic family (which margins to fix follows from the estimand), §8 rung-1 composition paragraph (reweight policy-moved mix away, usage target per ledger). Build clean, 15 pp, table resolves, lint clean but for one standing formal-prose contraction. Reason: the estimand ambiguity was the board's central (and Codex-rated "Major") finding; making pi primary + an explicit per-rung ledger resolves it consistently, at the cost of reversing the earlier mediator framing -- which was the right call, since policy-moved topic/profession mix genuinely isn't a usage effect.

2026-06-17 - SIMULATION GROUNDING done (board fix #2, UNANIMOUS 5/5). The §8.3 numbers were never fabricated -- RAN all three sims (simulations/rung1-ladder-sim.py, rung1-recovery-sim.py, rung-failure-map.py) and the prose values check out exactly: recovery naive +0.128 -> reweighted -0.000 on a pure composition artifact (and +0.145 under unmeasured drift); failure map 56%/99%/100% FP as hidden share climbs 0.3/0.6/0.9, 94% FN for sparse (n=40) cells, 8% FP for noisy exposure, 100% FP for selection->wave in 2-group. The board's actual complaint was AUDITABILITY -- a reader of the paper couldn't see the source. FIX: added Table~\ref{tab:failuremap} (operating characteristics, 9 scenarios x threat x misrouting rate, 400 reps), with code pointers in the caption (rung-failure-map.py) and in the recovery prose (rung1-recovery-sim.py); §8.3 prose now cites the table and uses the exact rates instead of "past half"/"nine in ten". Removed the "simulation spine" from the §8 TODO (the sims ARE built and run) -- the remaining TODO is now clearly-optional future extensions (Italian/Croatian-Serbian). Build clean, 16 pp (two new tables: estimand ledger + failure map), both resolve, lint clean but for one standing contraction. NOTE for later: reviewers (and source-grounding LAW) suggested Quarto so the numbers compute at render and can't drift; the static table + code pointers is the middle ground for now, full Quarto-ization is a bigger architectural change to weigh separately. Reason: both board must-fix items (estimand lock, sim grounding) are now closed; the §8.3 numbers are auditable against committed code, closing the source-grounding gap the whole paper preaches against.

2026-06-17 - §7 MULTILEVEL FIX + the outstanding items from the two earlier external reviews (after shipping b98aac1). (a) §7 "why DiD not a multilevel model" REWRITTEN per Gelman: the old para claimed DiD "differences the unobserved confound away", which contradicts §5/§8's "no estimator removes selection-into-timing"; now concedes differencing clears only TIME-INVARIANT confounds and leaves the trend confound exactly where a model would, so the identifying assumption is IDENTICAL either way (parallel trends here, a counterfactual-trend term there) and a pre-period checks both equally -- DiD's advantage is "a named and conventional" assumption (transparency), NOT a stronger guarantee. (b) Item 1 (Madame, flagged by both reviews + Keith) -- §8 rung 1 now states the two costs: the Madame+title cue narrows the estimand to a formal title-address context (not profession nouns at large), and Madame-implies-female-referent is itself a classifier whose error drifts into b if Madame usage drifts. (c) Item 2 (review 1) -- §8 rung 2 now has the not-yet-treated ASYMMETRY as a lesson: France 1998 has no not-yet-treated peer inside the four, Québec (1979) is treated for most of the window, so the estimator has fewer clean comparisons than four polities suggest ("more varieties bought not more identification but a thinner set of usable contrasts"). (d) Item 3 (review 1) -- §4 new DENOMINATOR paragraph (proportion of what, weighted how, over which referent distribution; the denominator is where composition re-enters, so it belongs in the estimand). (e) Item 4 (review 1 + Eisenstein) -- §4 classifier-eval PRESCRIPTION: evaluate accuracy by period and variety, not globally (differential error = the measurement twin of a non-parallel trend). (f) Item 5 (review 1) -- §2 renamed "clustering unit" -> "dependence unit" and planted the few-clusters-asymptotics-fail point early (ledger table label updated to match). (g) Optional cheap adds: §8 rung 2 "wave" now glossed once formally (a secular gender-ideology trend that moves timing and usage alike); §8 rung 3 explicitly names the no-untreated-control problem -> "an outside, language-matched comparison or a triple difference", labelling rung 3 a design variant not a third two-group DiD. Build clean, 17 pp, all refs/cites resolve, lint clean but for one standing formal-prose contraction. STILL OPEN (mixed-board items NOT from the two earlier reviews, out of this turn's scope): Roth's operationalize-the-ladder-thresholds and temper-§7-inference-remedies; Eisenstein's diffusion-precondition + leave-one-author-out + per-strategy relative-timing-as-diagnostic. Reason: closes the §7 self-contradiction and every substantive outstanding point from the two earlier external reviews; the paper now answers all but the newest (mixed-board) secondary suggestions.

2026-06-17 - Remaining MIXED-BOARD secondary items cleared (after shipping d3cc993), completing the whole review cycle. (a) Roth, OPERATIONALIZE LADDER THRESHOLDS -- §6 sensitivity family now says the breakdown benchmark is "fixed in advance and measured, not felt": the largest trend swing in an untreated stretch or untreated variety sets the bar, and the reweightable margins are listed from domain knowledge before the estimate, so "plausibly" can't mean "whatever the result needs". (b) Roth, TEMPER §7 INFERENCE REMEDIES -- wild cluster bootstrap can under-cover with very few/unbalanced clusters and randomization inference needs a credible assignment model that politically chosen staggered adoption rarely supplies; "they sharpen the honesty of the interval; they do not manufacture one". (c) Eisenstein, DIFFUSION PRECONDITION -- new §3 paragraph: feminization is a gradual S-curve diffusion (weinreich1968, tagliamontedarcy2007), so a shock identifies only if the change responds as a level shift or acceleration, not the continuation of a curve already climbing; where it's smooth and in progress, parallel pre-trends never hold and "not identified" is modal BY CONSTRUCTION of the phenomenon; the clean cases are genuinely discrete shocks (hard enforcement date, orthographic reform, transcription flip). (d) Eisenstein, AUTHOR/TOPIC DEPENDENCE -- §6 third family now adds leave-one-author-out beside leave-one-source-out (a few prolific bylines or a topic burst can carry a token-level rate) + dedup syndicated reprints. (e) Eisenstein, PER-STRATEGY TIMING -- §6 fourth family reframes the strategy decomposition as a per-strategy event study whose relative timing is evidence: strategies sit on a cline (low-cost agreement la ministre early, high-cost derivation autrice late), so it says not just whether a policy bit but which strategy it moved. Build clean, 17 pp, lint clean. STATUS: every item from BOTH external reviews AND the mixed Codex/Opus board is now addressed; the paper is a thoroughly-revised complete draft. Reason: the full review cycle (two external reviews + 5-seat mixed board) is closed; natural next gates are a fresh review pass or /proofread, or Brett's own read.

2026-06-17 - /proofread run (read-only audit) then its fixes applied. Build was already clean (0 undefined refs, 26 cites resolve), so the audit was consistency/quality not triage. FIXES: (#1 major) ACKNOWLEDGEMENTS filled from a TODO stub -- "The large language models Claude Opus 4.8 and OpenAI Codex served as drafting and editing aids ... including a simulated expert review board used to stress-test the design. I am responsible for all theoretical claims, arguments, errors, and interpretive choices." (project's default AI-ack form; only the models actually used). (#2 major) TWO DIFFERENT "FIVES" -- the abstract/§1 "five things to separate" (population/composition/measurement/identifying/dependence) collided with §2's different "five units" taxonomy (linguistic/measurement/aggregation/identifying/dependence); dropped the numeral in abstract+§1 ("several things") so only §2 owns "five units", removing the false parallel. (#3 major) DiD ABBREVIATION -- the §2 section heading and both tables already used "DiD" undefined while prose spelled out "difference-in-differences" 23x; now defined "difference-in-differences (DiD)" at the §1 first mention and abbreviated throughout the body, with title, pdfkeywords, visible keywords, and the abstract's single use kept spelled out. (#4 minor) COMMA SPLICE §8 ("...parallel trends in the rate, it does not replace it" -> "...rather than replacing it"). SKIPPED as deliberate/optional: the one standing formal-prose "this is a falsification" contraction; the "wearing the costume of an effect" metaphor; "parallel trends ... is its price" singular verb; §2's forward-ref to Table 1. Build clean, 17 pp, lint clean. Reason: closes the proofread's actionable items; the draft is now internally consistent (one taxonomy owns "five", DiD defined-then-abbreviated) and the AI acknowledgement is no longer a stub.

2026-06-17 - THIRD external-review round (two more detailed reviewers, pasted by Brett). KEYSTONE finding investigated + resolved: read simulations/rung-failure-map.py -- the ladder's `verdict()` DOES apply a sensitivity/calibration gate (the placebo-calibrated `floor_band`: bounded only if |DiD| > 1.5*max(placebo-date DiD)+0.01), so the reviewer's case (a) holds, NOT case (b). Hidden drift (a level shift at T0) cancels inside the placebo windows, so the benchmark stays low while the real contrast jumps -> the 56/99/100% FP IS the gate failing for the reviewer's exact reason: a benchmark built from windows the drift doesn't touch can't bound it. RECONCILED §6 (added the benchmark's blind spot: catches only violations that leave a footprint in its windows; drift confined to the treated post-period or that inflates the benchmark passes) + §8.3 (states the gate fails for that reason, "made quantitative", + a DGP-description sentence: two varieties, twenty years, logistic cell proportions, hidden-share/n=40/misroute defined). ALSO APPLIED (clearly-correct, both reviewers): §3 S-CURVE fix -- "smooth/already-in-progress -> PT never holds" was WRONG (parallel S-curves satisfy PT); recast as DIFFERENTIAL PHASE (varieties at different curve positions have different untreated slopes; linear extrapolation books curvature as effect) + transcription-flip is a clean MEASUREMENT shock not a usage effect; §5 two overclaims fixed ("move while pi stays put" -> "be mistaken for the policy's effect on pi"; "a diagnostic that can expose it" -> "...when the relevant margin is measured, or at least bound the claim's fragility"); §2 "the second is contamination" -> "For a usage-rate estimand, the second is contamination"; §8 rung 2 "wave that constitutes the effect" -> "that the design must not mistake for the effect"; §7 inference -> "more illustrative than runnable ... report the absence of a clean interval as part of the result"; §6 pre-registered->pre-specified, "the same"->"of the same form"; §8 rung 1 spoken-series reframed as out-of-reach-by-construction (dropped the "added later" promise that never pays); "four or five clusters"->"four". TABLE 1 fixed: Control row -> Comparison; rung-3 cells (treatment "no within-Switzerland contrast", outcome "language-specific marking, harmonized", comparison "not untreated controls"); dependence "(four)". FLOATS fixed (serious): added placeins + \FloatBarrier at §8 start and before §8.3 (tables no longer float into §7/Rung 2) + \clearpage before §9 (fixes the "9 CONCLUSION" header bleed -- verified §8 headers now pp.10-15, §9 on p.16). VERIFIED both Légifrance JORFTEXT IDs resolve (reviewer's top source worry). Build clean, 18 pp, lint clean but for the one standing contraction. STILL OPEN (Tier 2, this review): §2 "bias term"->"corpus-filter term" rename + sampling-variation-vs-b sentence + level-dependent-proportion-bias sentence (b=alpha-pi(alpha+beta)); §4 la-table-verte in-domain example; §5/figure label alignment (prose "T->C" vs node "Policy timing") + interference both-directions; §8 rung-3 triple-difference held-back sentence; abstract "every shock moves frequency" soften + §1 "not a sample"->"not simply" + "six questions in turn" order; Table 2 Monte-Carlo-error note (400 reps ~2.5pt); double-check Egami eLocator + Borusyak REStud vol/yr. Reason: answered the reviewer's load-bearing question (the gate fires and fails, a finding not a bug) and applied the argument-level corrections + the must-fix float/table repairs; the remaining Tier-2 items are local polish.

2026-06-17 - TIER-2 cleared + CLAUDISM sweep (Brett: clear Tier 2 + remove Claudisms before shipping). TIER 2: §2 "bias term"->"corpus-filter term" (reserve bias for outside-target) + a sampling-variation-vs-b sentence (b = the filter, not finite-corpus noise; cell-level uncertainty carries the latter); §4 level-dependent-proportion-bias sentence (fixed classifier error rates still give a bias that tracks pi, coupling the two PT assumptions in mechanism) + replaced the out-of-domain "la table verte" ceiling example with an in-domain one (une infirmiere competente, an already-feminine title); §5 figure node "Policy timing"->"Policy T" (aligns the prose's "T->C" with the diagram) + interference now stated both directions (treated->control's post-period too, slight here only because France dwarfs Suisse romande); §8 rung-3 triple-difference held-back sentence (German/Italian arms have no francophone-wave-matched control, so the third difference wouldn't subtract the wave cleanly); abstract "a form's frequency moves"->"may move"; §1 "not a sample"->"not simply a sample" + "the paper takes them in turn"->"taken up across the sections that follow" (the six questions don't map to sequential sections); Table 2 caption notes it's seeded with ~a couple points of Monte Carlo error. VERIFIED Egami (Sci Adv, doi 10.1126/sciadv.abg2652) and Borusyak (REStud 91(6):3253-3285, 2024) -- both correct, no bib change. CLAUDISMS removed: "wearing the costume of an effect" (§6), "earns its keep" (§9), "not merely whether a gap opens" (§8), "the very mix"/"the very thing" (§5/§8), and cut "honest" from 9 uses to 6 (the clustered "honest verdict"/"honest output" repetitions; kept the varied senses). Build clean, 18 pp, lint clean but for the one standing formal-prose contraction. Reason: the third review cycle is now fully closed (keystone + argument-level + presentation + Tier-2 polish) and the prose is de-tic'd; ready to ship.

2026-06-18 - LEVEL/CATEGORY-ERROR audit (Brett: "look for level/category errors"; per the CLAUDE.md explanation-level discipline + CGEL category/function rules). Found and fixed a cluster of MEREOLOGICAL slips (higher cognition attributed to an inanimate method/design/data/figure -- the "grammars do not judge" error): §8 "a design that reports an effect at 1986 is reading something other than the policy" -> "an effect that turns up at 1986 reflects..."; "the design must not mistake [the wave] for the effect" -> "that must be told apart from the effect"; "the design has caught the wave" -> "the contrast reflects that wave"; §8.3 "the case the method is honestly fooled by" -> "the case the design cannot catch"; §9 "drift the data can't see" -> "drift that goes unmeasured"; §5 caption "what the figure cannot draw" -> "cannot show". One CATEGORY/FUNCTION slip: §8 "endorsed feminine profession nouns" (conflating the morphosyntactic form/process with a gender category) -> "feminizing profession nouns". Two explicit FILLER VERBS off Brett's avoid-list: §5 "ideology that drives usage" -> "changes usage"; §8 "the trend that moves adoption timing and usage alike" -> "behind both". DELIBERATELY KEPT as legitimate idiom: "the design identifies" (identification is a property of the design, standard econometrics), methods that "falsify / expose / detect / catch / route / flag" (detection/mechanical, not higher cognition), "the policy moves the mix" and "a shock can move pi or b" (causal with implied mechanism), "ideology" as a latent DAG node / common cause (the §5 graph formalizes Ideology->pi/M/T; the prose mirrors the graph), and "what the data support" (standard idiom). Checked the rest: the form(feminine marking)/gender/referent(female) and grammatical-vs-social-gender distinctions are otherwise clean, and the explanatory-level discipline (usage pi vs corpus-distributional f vs institutional policy vs normative ideology) -- which IS the paper's thesis -- holds with no estimand-level slides. Build clean, 18 pp, lint clean but for the one standing contraction. Reason: the slips were genuine level/category errors by the project's own discipline; fixing them removes the irony of a level-discipline paper committing level errors, while leaving the technical idioms intact.

2026-06-18 - PHILOSOPHICAL category-error + fallacy audit (Brett). (a) Rylean CATEGORY MISTAKE found + fixed: §6/§9 listed the ladder outcomes as "the apparent change IS a shared trend, a measurement shift, ..., or NOT IDENTIFIED" -- conjoining ontological re-attributions (what the change is) with an epistemic status (whether we can tell), a heterogeneous-disjunction category mix; reworded so the epistemic outcome sits in a separate that-clause ("or else that no usage effect can be identified", "or show that it can't be identified"). (b) Genuine FALLACY found + fixed: the normative slogan "a downgrade is a result, not a failure ... reaching one counts as success" (§9) equivocated warranted-caution with correctness -- a "not identified" verdict that misses a real effect is a FALSE NEGATIVE (Table 2: 94% FN for a small effect in a sparse variety), i.e. a power failure, not success. Reworded §9: "an answer worth reporting, not a result to bury. But honesty cuts both ways: a downgrade earned against the evidence is a finding, while the same verdict forced only by thin data is a missed effect, not a success." (c) Added TWO sentences naming under-acknowledged tensions (Brett asked to add them): §6 SKEPTICAL-REGRESS ("the defeater itself needs discipline" -- an always-conceivable local-wave defeater would collapse the ladder to a single rung unless the defeater is credible in the case, not merely conceivable, so no case clears the bar) and §6 ELIMINATIVE-INDUCTION ("the bound is eliminative ... a bounded claim is always made against the imaginable confounds, not all the possible ones" -- the unconceived-alternatives point, of which hidden drift is a worked instance). CLEARED on inspection (not errors): no affirming-the-consequent (calendar test pre-demoted to a partial filter; France-1986 placebo is clean modus tollens), no prosecutor's fallacy (Table 2 reports P(verdict|truth), never inverted), anti-operationalist (f != pi; outcome a bundle), counterfactual-agnostic (PT framed as unverifiable), is/ought openly epistemic-normative. Build clean, 18 pp, lint clean. Reason: one real category mistake + one real fallacy repaired, two deep tensions named (turning referee-bait into stated positions); the paper is now philosophically buttoned up.
